workflow:
  steps:
    - rebuild_package:
        project: home:mx-packaging
        package: mx-installer 
  filters:
    event: push
    branches:
        only:
            - main
            - master
