workflow:
  steps:
    - trigger_services:
        project: home:mx-packaging
        package: mx-installer
  filters:
    event: push
    branches:
        only:
            - main
            - master
